apiVersion: v1
kind: Pod
metadata:
  name: workerapp-pod
  labels:
    name: workerapp-pod
    app: demo-votingapp
spec:
  containers:
    - name: workerapp
      image: dockersamples/examplevotingapp_worker
